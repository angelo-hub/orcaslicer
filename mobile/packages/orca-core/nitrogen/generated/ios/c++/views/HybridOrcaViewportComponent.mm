///
/// HybridOrcaViewportComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridOrcaViewportComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridOrcaViewportSpecSwift.hpp"
#import "NitroOrcaCore-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::orca;
using namespace margelo::nitro::orca::views;

/**
 * Represents the React Native View holder for the Nitro "OrcaViewport" HybridView.
 */
@interface HybridOrcaViewportComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridOrcaViewportComponent {
  std::shared_ptr<HybridOrcaViewportSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridOrcaViewportComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridOrcaViewportComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridOrcaViewportShadowNode::defaultSharedProps();
    std::shared_ptr<HybridOrcaViewportSpec> hybridView = NitroOrcaCore::NitroOrcaCoreAutolinking::createOrcaViewport();
    _hybridView = std::dynamic_pointer_cast<HybridOrcaViewportSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroOrcaCore::HybridOrcaViewportSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroOrcaCore::HybridOrcaViewportSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridOrcaViewportProps>(props);
  const auto* oldViewProps = static_cast<const HybridOrcaViewportProps*>(oldProps.get());
  NitroOrcaCore::HybridOrcaViewportSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // sessionId: number
    if (oldViewProps == nullptr
          ? newViewProps.sessionId.isProvided()
          : !newViewProps.sessionId.hasSameValue(oldViewProps->sessionId)) {
      swiftPart.setSessionId(newViewProps.sessionId.get());
    }
    // mode: enum
    if (oldViewProps == nullptr
          ? newViewProps.mode.isProvided()
          : !newViewProps.mode.hasSameValue(oldViewProps->mode)) {
      swiftPart.setMode(static_cast<int>(newViewProps.mode.get()));
    }
    // maxLayer: number
    if (oldViewProps == nullptr
          ? newViewProps.maxLayer.isProvided()
          : !newViewProps.maxLayer.hasSameValue(oldViewProps->maxLayer)) {
      swiftPart.setMaxLayer(newViewProps.maxLayer.get());
    }
    // showTravels: boolean
    if (oldViewProps == nullptr
          ? newViewProps.showTravels.isProvided()
          : !newViewProps.showTravels.hasSameValue(oldViewProps->showTravels)) {
      swiftPart.setShowTravels(newViewProps.showTravels.get());
    }
    // revision: number
    if (oldViewProps == nullptr
          ? newViewProps.revision.isProvided()
          : !newViewProps.revision.hasSameValue(oldViewProps->revision)) {
      swiftPart.setRevision(newViewProps.revision.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroOrcaCore::NitroOrcaCoreAutolinking::isOrcaViewportRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroOrcaCore::HybridOrcaViewportSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
